# Production build: talks to the real server (same origin by default).
VITE_DEMO_MODE=false
# Only chat + movie night are open for now; everything else shows as "coming later".
VITE_RELATIONSHIP=friends
VITE_FEATURES=chat,movie
